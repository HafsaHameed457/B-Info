// if one disk move simply from first to last 
// Shift n-1 disks from A to B using C (req)
// Shift last disk from A to C
// Shift n-1 disks from B to C using A (req)

const towerOfHanoi=(n,rod1,rod3,rod2)=>{
while(rod1.length||rod2.length){
   if(rod1.length&&!rod3.length){
    rod3.push(rod1[0])
   }
   else{

   }
}
}